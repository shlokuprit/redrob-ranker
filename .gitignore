# Large data — do not commit the candidate pool
data/candidates.jsonl
data/candidates.jsonl.gz
data/*.json
!data/my_labels.example.json

# Precomputed artifacts (regenerate with embed.py, or use Git LFS if you must commit)
artifacts/*.npy
artifacts/*.pkl
artifacts/*.json

# Output
output/*.csv

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.DS_Store
